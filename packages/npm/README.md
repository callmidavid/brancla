# Brancla CLI

Install globally with npm:

```bash
npm install -g brancla
```

Then run Brancla in any git repository:

```bash
brancla scan
brancla clean --dry-run
brancla clean
```

The npm package downloads the prebuilt Brancla binary for your OS from GitHub Releases. Go is not required.
